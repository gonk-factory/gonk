# Test-only dependencies for the trajectory collector (gonk-p8j).
# The collector itself imports nothing outside the standard library on purpose:
# it runs inside the LiteLLM proxy image, which gonk does not build, so it must
# not add a runtime dependency to somebody else's container.
pytest==8.3.4
